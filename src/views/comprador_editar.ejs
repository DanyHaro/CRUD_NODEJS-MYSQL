<%-include('partes/header')%>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/update/<%= data.id %>" method="post">

                            <div class="form-group">
                                <!--name="nombre" deben ser igual que los atributos creados en la BD, ya que pasaremos los datos desde el frontend a la BD-->
                                <input autocomplete="off" type="text" value="<%=data.nombre%>" name="nombre" class="form-control">
                            </div>

                            <div class="form-group">
                                <!--name="nombre" deben ser igual que los atributos creados en la BD, ya que pasaremos los datos desde el frontend a la BD-->
                                <input autocomplete="off" type="text" value="<%=data.direccion%>" name="direccion" class="form-control">
                            </div>

                            <div class="form-group">
                                <!--name="nombre" deben ser igual que los atributos creados en la BD, ya que pasaremos los datos desde el frontend a la BD-->
                                <input autocomplete="off" type="text" value="<%=data.telefono%>" name="telefono" class="form-control">
                            </div>
                            <button type="submit" class="btn btn-primary">Editar comprador</button>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>













    <%-include('partes/footer')%>